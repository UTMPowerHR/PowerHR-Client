import Button from '@mui/material/Button';

const ChoiceTemplateButton = () => {
    return (
        <div>
            <Button variant="contained" color="primary">
                Choose Template
            </Button>
        </div>
    );
};

export default ChoiceTemplateButton;
